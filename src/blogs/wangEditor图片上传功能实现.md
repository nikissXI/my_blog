---
title: "wangEditor图片上传功能实现"
keywords: "wangEditor,php"
date: 2021-06-24
---

<h4 id="0pckl">官方文档<br/><a href="http://www.wangeditor.com/doc/" target="_blank"><font color="#46acc8">http://www.wangeditor.com/doc/</font><span></span></a></h4><h4 id="0pckl"><br/>前端代码</h4><pre><code>&lt;script src="http://cdn.jsdelivr.net/npm/wangeditor@latest/dist/wangEditor.min.js"&gt;&lt;/script&gt;rn&lt;script src="http://cdn.bootcss.com/jquery/3.2.1/jquery.min.js"&gt;&lt;/script&gt;rn&lt;!-- 引入 wangEditor.min.js --&gt;rn&lt;script type="text/javascript"&gt;rn    const E = window.wangEditorrn    const editor = new E('#weditor')rn    const $text1 = $('#content')rn    editor.config.onchange = function(html) {rn        // 第二步，监控变化，同步更新到 textarearn        // html = html.replace('xmp', 'pre')rn        $text1.val(html)rn    }rn    editor.config.uploadImgMaxSize = 10 * 9999 * 9999 // 10Mrn    editor.config.uploadImgServer = 'upload.php' //设置上传文件的服务器路径rn    editor.config.uploadFileName = "file"; //文件名称  也就是你在后台接受的 参数值rn    editor.config.uploadImgHeaders = { //header头信息 rn        'Accept': 'text/x-json'rn    }rn    editor.create()rn    // 第一步，初始化 textarea 的值rn    $text1.val(editor.txt.html())rn    // editor.config.debug = true;rnrn    //上传图片的回调函数rn    editor.config.uploadImgHooks = {rn        // 图片上传并返回了结果，图片插入已成功rn        success: function(xhr) {rn            console.log('success', xhr)rn        },rn        // 图片上传并返回了结果，但图片插入时出错了rn        fail: function(xhr, editor, resData) {rn            console.log('fail', resData)rn        },rn        // 上传图片出错，一般为 http 请求的错误rn        error: function(xhr, editor, resData) {rn            console.log('error', xhr, resData)rn        },rn        customInsert: function(insertImgFn, result) {rn            // result 即服务端返回的接口rn            console.log('customInsert', result.data[0].url)rn            // insertImgFn 可把图片插入到编辑器，传入图片 src ，执行函数即可rn            insertImgFn(result.data[0].url)rn        }rn    }rn&lt;/script&gt;</code></pre><p><br/></p><h4 id="0pckl">后端代码(upload.php)<br/></h4><pre><code class="PHP"><xmp><?phprnheader('Content-Type:application/json; charset=utf-8');rnfunction images()rn{rn    //图片文件的生成rn    $savename = date('YmdHis', time()) . mt_rand(0, 9999) . '.jpeg';rn    //生成文件夹rn    $imgdirs = "upload/image/" . date('Y-m-d', time()) . '/';rn    mkdirs($imgdirs);rn    //图片保存的路径rn    $savepath =  $imgdirs . $savename;rn    $url = "http://nikiss.top";rn    //生成一个URL获取图片的地址rn    //返回数据。wangeditor3 需要用到的数据 json格式的rn    $imgdata = array();rn    $imgdata[] = array('url' => $url);rn    $data = array('errno' => 0, 'data' => $imgdata);rn    move_uploaded_file($_FILES["file"]["tmp_name"], $savepath);rn    //返回数据rn    echo json_encode($data);rn}rn//创建文件夹 权限问题rnfunction mkdirs($dir, $mode = 0777)rn{rn    if (is_dir($dir) || @mkdir($dir, $mode)) return TRUE;rn    if (!mkdirs(dirname($dir), $mode)) return FALSE;rn    return @mkdir($dir, $mode);rn}rnimages();rn</xmp></code></pre><p>后端接口就是把发过来的图片，保存到目标路径，然后把图片路径返回回去给前端，就这样<br/></p>