---
title: "用Linuxdeploy在安卓手机装Linux"
category: "运维"
keywords: "Linuxdeploy 安卓"
date: 2023-02-28
summary: "唉，就是玩"
---

<p style="line-height:1.15;">整这玩意你就可以把手机当服务器跑了，还不会影响手机的正常使用，在上面装渗透工具然后插个非实名卡就可以近源渗透啦，非常的刑！</p><p style="line-height:1.15;">当然还可以做很多正常的骚操作，比如跑脚本、挂QQ机器人、开MC服（狗头）</p><p style="line-height:1.15;"><br/></p><h2 id="qbx1e"><font size="5" color="#7b5ba1">Linuxdeploy安装配置</font></h2><p style="line-height:1.15;"><b><font color="#c24f4a">用这个app得先root了！</font></b></p><p style="line-height:1.15;">app下载链接 <a href="https://github.com/meefik/linuxdeploy/releases"><font color="#46acc8">https://github.com/meefik/linuxdeploy/releases</font><span></span></a></p><p style="line-height:1.15;">rnrn</p><p style="line-height:1.15;">当前最新版本[2.6.0] - 2020-02-01，大概率是停更了？[2.6.0] - 2020-02-01，大概率是停更了？<br/></p><p style="line-height:1.15;"><br/></p><h3 id="1kyyh"><font size="5" color="#7b5ba1">Linux Deploy介绍</font></h3><p style="line-height:1.15;">Linux Deploy是一款运行在安卓平台上, 获取root权限利用chroot技术安装lxc版linux系统的软件。如果系统安卓版本较旧，可能需要安装busybox添加bin下的命令, 除此之外由于需要使用底层的指令运行脚本, 因此要求有root权限。LinuxrnDeploy支持大部分主流Linux发行版。</p><p style="line-height:1.15;"><b>安装方法有三种，本文只讲内置仓库和rootfs.tar文件安装：</b></p><p style="line-height:1.15;"><!--[if-->1、 <!--[endif]-->在线安装，直接从app内置仓库拉取系统进行安装，安装简单，可以一键安装部署使用，但里面的系统比较旧并且选择少；</p><p style="line-height:1.15;"><!--[if-->2、 rootfs.tar安装（个人推荐），自行下载rootfs.tar文件进行安装，可以下载安装自己想要的系统，流行的Linux发行版都有，下面以下载ubuntu为例：</p><p style=" padding-left:2em;line-height:1.15;">打开lxc镜像站 <a href="https://mirrors.bfsu.edu.cn/lxc-images/images/"><font color="#46acc8">https://mirrors.bfsu.edu.cn/lxc-images/images/</font></a> ，搜索ubuntu并打开，选择发行版（如jammy），架构，cloud，选择最新时间的（如果出问题可以试试之前的），点击 rootfs.tar.xzrn下载，最终链接就是（这个链接会404的，得自己去翻最新的）</p><p style=" padding-left:2em;line-height:1.15;">https://mirrors.bfsu.edu.cn/lxc-images/images/ubuntu/jammy/arm64/cloud/20230223_07%3A43/rootfs.tar.xz</p><p style="line-height:1.15;">rnrnrnrnrnrnrnrnrnrnrnrn</p><p style="line-height:1.15;">3、 Docker安装，还没试过，可以自己试试（手动狗头）。3、 Docker安装，还没试过，可以自己试试（手动狗头）。3、 Docker安装，还没试过，可以自己试试（手动狗头）。<br/></p><p><br/></p><h3 id="dv2cz"><font size="5" color="#7b5ba1">运行环境配置</font></h3><p style="padding-left:2em;">注：在新版本(linux Deploy2.6及以上)不需要打开设置配置环境, 如果安卓版本比较旧或者软件版本较旧可能需要先使用busybox更新linux环境并在这里更新环境变量为busybox安装路径。</p><p style="padding-left:2em;">左上角选项 – 设置，把 锁定Wi-Fi、CPU唤醒、联网更新 勾上；开机自启动看自己的需求；telnet和httpd暂时用不到，开不开都行。</p><p><br/></p><h3 id="fvith"><font size="5" color="#7b5ba1">系统安装（在线安装）</font></h3><p><!--[if-->1)&nbsp;&nbsp;rn<!--[endif]-->发行版：自行选择</p><p><!--[if-->2)&nbsp;&nbsp;rn<!--[endif]-->架构：看你处理器架构，不清楚自己查</p><p><!--[if-->3)&nbsp;&nbsp;rn<!--[endif]-->发行版本：自行选择，这里显示的都是对应版本的代号，不清楚自己查</p><p><!--[if-->4)&nbsp;&nbsp;rn<!--[endif]-->源地址：国内源推荐，这里列举两个</p><p>Debian: http://mirrors.ustc.edu.cn/debian/</p><p>Ubuntu: https://mirrors.bfsu.edu.cn/ubuntu-ports/</p><p><!--[if-->5)&nbsp;&nbsp;rn<!--[endif]-->安装类型：镜像文件，目录，分区，RAM(不建议)</p><p><!--[if-->6)&nbsp;&nbsp;rn<!--[endif]-->安装路径: 这里需要看安装类型确定, 这里给出三种:</p><p>   a.镜像安装，就是系统镜像的路径，如rn${EXTERNAL_STORAGE}/debian.img，镜像建议分配不小于1024，因为这个镜像大小就是你系统可用空间大小，不够的话可以调，下面会说。</p><p>   b.目录，最好选择data目录下的路径如/data/debian, 如果没有该文件夹会自动创建该文件夹，系统可用空间与手机共享。</p><p>   c.分区，需要设置安装路径。sd卡路径为/dev/block/mmcblk(xpy), 其中 x 和 y 是数字。可以通过在本地终端或者 adb shell进入终端使用命令 df -ah查看挂载点, ls /dev/block/mmcblk*查看所有 mmcblk 这样的块设备</p><p><!--[if-->7)&nbsp;&nbsp;rn<!--[endif]-->文件系统：ext4</p><p><!--[if-->8)&nbsp;&nbsp;rn<!--[endif]-->用户名：不要写root就行</p><p><!--[if-->9)&nbsp;&nbsp;rn<!--[endif]-->用户密码：要我帮你设置吗</p><p><!--[if-->10) <!--[endif]-->特权用户：不要动</p><p><!--[if-->11) <!--[endif]-->本地化：就是语言，如果要中文就选zh_CN.UTF-8，否则就保持C</p><p><!--[if-->12) <!--[endif]-->挂载点：将手机目录挂在载安装的Linux下某个目录,，类似docker的目录映射</p><p><!--[if-->13) <!--[endif]-->SSH：如果要用SSH连就勾上</p><p><!--[if-->14) <!--[endif]-->声音服务：随你</p><p><!--[if-->15) <!--[endif]-->GUI：看自己需求, 不是很流畅, 建议使用vnc +rnxfce</p><p><!--[if-->16) <!--[endif]-->回到主界面，点右上角的三个点，点击安装，等到出现 &lt;&lt;&lt;deployrn就是安装好了，看看输出的内容有没有出现failed。如果ssh服务failed了，就用telnet或httpd进去安装ssh，进去方法下面会说。</p><p>rnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrn</p><p><!--[if-->17) <!--[endif]-->安装完成后请先点击停止卸载容器, 然后再点击开始看到最后出现 &lt;&lt;&lt;deploy 并且没有 fail 即成功。此时可以通过 telnet 或者 SSH 登录容器。如果ssh启动失败，就看《无SSH进入系统》。</p><p><br/></p><h3 id="lxwzr"><font size="5" color="#7b5ba1">系统安装（rootfs.tar安装）</font></h3><p><!--[if-->1)&nbsp;&nbsp;rn<!--[endif]-->发行版：rootfs.tar</p><p><!--[if-->2)&nbsp;&nbsp;rn<!--[endif]-->源地址：把下载的rootfs.tar文件改名为rootfs.tar，因为下载的文件名不一定叫这个，然后把它放到系统文件管理的根目录，因此这个位置就填${EXTERNAL_STORAGE}/rootfs.tar，你要是想放其他地方或改其他名字这里跟着改</p><p><!--[if-->3)&nbsp;&nbsp;rn<!--[endif]-->安装类型：镜像文件，目录，分区，RAM(不建议)</p><p><!--[if-->4)&nbsp;&nbsp;rn<!--[endif]-->安装路径: 这里需要看安装类型确定, 这里给出三种:</p><p>   a.镜像安装，就是系统镜像的路径，如rn${EXTERNAL_STORAGE}/debian.img，镜像建议分配不小于1024，因为这个镜像大小就是你系统可用空间大小，不够的话可以调，下面会说。</p><p>   b.目录，最好选择data目录下的路径如/data/debian, 如果没有该文件夹会自动创建该文件夹，系统可用空间与手机共享。</p><p>   c.分区，需要设置安装路径。sd卡路径为/dev/block/mmcblk(xpy), 其中 x 和 y 是数字。可以通过在本地终端或者 adb shell进入终端使用命令 df -ah查看挂载点, ls /dev/block/mmcblk*查看所有 mmcblk 这样的块设备</p><p><!--[if-->5)&nbsp;&nbsp;rn<!--[endif]-->文件系统：ext4</p><p><!--[if-->6)&nbsp;&nbsp;rn<!--[endif]-->用户名：不要写root就行</p><p><!--[if-->7)&nbsp;&nbsp;rn<!--[endif]-->用户密码：要我帮你设置吗</p><p><!--[if-->8)&nbsp;&nbsp;rn<!--[endif]-->特权用户：不要动</p><p><!--[if-->9)&nbsp;&nbsp;rn<!--[endif]-->本地化：就是语言，如果要中文就选zh_CN.UTF-8，否则就保持C</p><p><!--[if-->10) <!--[endif]-->挂载点：将手机目录挂在载安装的Linux下某个目录,，类似docker的目录映射</p><p><!--[if-->11) <!--[endif]-->SSH：这个可以试试勾选，不过99%概率rootfs.tar包里是没有ssh的，因此用不了</p><p><!--[if-->12) <!--[endif]-->声音服务：随你</p><p><!--[if-->13) <!--[endif]-->GUI：不要选，因为包里100%没有</p><p>rnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrnrn</p><p><!--[if-->14) <!--[endif]-->回到主界面，点右上角的三个点，点击安装，等到出现 &lt;&lt;&lt;deployrn就是安装好了，看看输出的内容有没有出现failed。没有就可以进行连接了。连接方式看《无SSH进入系统》</p><p><br/></p><h3><font id="8eaei" size="5" color="#7b5ba1">进入手机终端</font></h3><p>左上角选项 – 设置，把TELNET或HTTP勾选，哪个方便用哪个，如果是开HTTP，左上角选项 – 终端，就会打开浏览器进去手机终端了。</p><p>进去后输入命令su，切换为root再操作</p><p><br/></p><h3><font size="5" id="d6mfg" color="#7b5ba1">无SSH进入系统</font></h3><p>进入手机终端后，输入以下命令进入安装的系统</p><p><code>/data/user/0/ru.meefik.linuxdeploy/files/bin/linuxdeployrnshell -u root</code></p><p>出现 root@localhost:~# 就对了</p><p>Debian系的系统就输入以下命令安装ssh，怎么配置或其他系统自己解决啦</p><p><code>apt installrnopenssh-server</code></p><p>然后可以可以把SSH服务勾上了</p><p><br/></p><h3><font size="5" id="f08bb" color="#7b5ba1">非root的Tab补全无效</font></h3><p>root登陆后输入</p><p><code>ln -sf /bin/bash /bin/sh</code></p><p>软链接bash和sh, 或者网页搜索rn&nbsp;切换默认sh为bash</p><p><br/></p><h3><font size="5" id="q2uvr" style="" color="#7b5ba1">修复apt错误</font></h3><p>使用vi修改/etc/passwd中_apt的65535为3003。rn</p><p><code>vi /etc/passwd</code></p><p>再输入 <code>/_apt</code> 再按enter, 再按 i 键变输入模式, 修改65535为3003, 再按ESC键最后再按 <code>:wq</code> 回车保存即可</p><p><br/></p><h3><font size="5" id="wticy" color="#7b5ba1">修复DNS解析错误</font></h3><p>输入以下命令删除/etc/resolv.conf</p><p><code>rmrn/etc/resolv.conf</code></p><p>再输入以下命令创建resolv.conf并赋予755权限</p><p><code>touchrn/etc/resolv.conf &amp;&amp; chmod 755 /etc/resolv.conf</code></p><p>然后停止容器再启动，看看DNS解析能用没，如果还不能用就再操作操作一遍</p><p><br/></p><h3><font size="5" id="tz02v" color="#7b5ba1">改软件源为国内镜像站</font></h3><p>这个站挺好用的，北京外国语大学开源软件镜像站，用ctrl+F搜对应的系统名关键字就有了</p><p><a href="https://mirrors.bfsu.edu.cn/help/"><font color="#46acc8">https://mirrors.bfsu.edu.cn/help/</font><span></span></a></p><p><br/></p><h3><font size="5" id="nh3fb" color="#7b5ba1">用户无法使用ping</font></h3><p>root登陆后输入</p><p><code>usermod -G 3003 root</code></p><p>然后重启容器解决。</p><p>其他的用户将命令里的root改为新创建的用户名即可。</p><p><br/></p><h3><font size="5" id="mocy9" color="#7b5ba1">Tab补全不好使</font></h3><p>Debian系的系统就输入以下命令安装ssh，其他系统自己解决啦</p><p><code>apt install bash-completion</code></p><p><br/></p><h3><font size="5" id="ghwig" color="#7b5ba1">系统开机服务自启动</font></h3><p>APP右下角设置，把初始化的启用勾上，其他不要动</p><p>然后进系统输入以下命令</p><p><code>touch /etc/rc.localrn&amp;&amp; chmod 777 /etc/ rc.local</code></p><p>然后在里面写命令就行，比如开机启动cron服务</p><p><code>/etc/init.d/cron start</code></p><p><br/></p><h3><font size="5" id="iqldk" color="#7b5ba1">手机锁屏后响应变卡</font></h3><p>进入手机的终端，输入su进root，然后输入以下命令，可以阻止手机进入低功耗模式</p><p><code>echo ld &gt; /sys/power/wake_lock</code></p><p><br/></p><h3><font size="5" id="t1pxc" style="" color="#7b5ba1">重启系统</font></h3><p>由于系统是在容器里面运行的，类似于docker，所以没法在内部重启的，只能在APP里点停止再启动</p><p>或者用以下命令直接重启手机，Linuxdeploy记得设置 开机自动启动</p><p><code>unchroot am start -arnandroid.intent.action.REBOOT</code></p><p><br/></p><h3><font size="5" id="5lhd7" color="#7b5ba1">镜像文件（磁盘空间）扩容</font></h3><p>由于使用镜像文件方式安装，系统的磁盘空间就是img文件的大小，使用dd命令扩容即可</p><p>先进入手机终端，然后找到img文件的位置，就是安装位置</p><p>如果安装路径是 ${EXTERNAL_STORAGE}/debian.img，</p><p>那img文件绝对路径就是 /storage/emulated/0/linux/debian.img</p><p>可以先用cd命令看看是不是在那个地方</p><p>然后依次输入</p><p><code>dd if=/dev/zero bs=1048576rncount=1000 &gt;&gt; /storage/emulated/0/debian.img</code></p><p><code>e2fsck -frn/storage/emulated/0/kali.img</code></p><p><code>resize2fsrn/storage/emulated/0/kali.img</code></p><p>上面的代码依次输入完磁盘空间就会增加1G</p><p>可以自己改bs或count的值，查一下dd命令怎么用</p><p><br/></p><h3><font size="5" id="38hv7" color="#7b5ba1">外网怎么访问</font></h3><p>可以在系统里装个frp或nps搞端口映射，或者整vpn组网，方法很多</p><p><br/></p><h3><font color="#f9963b" size="5" id="jg0x8">本文引用文章列表如下，感谢他们的知识</font></h3><p style="line-height:1;"><a href="https://www.bilibili.com/video/BV19S4y1j7Ba/?vd_source=17a331d2470839258ad5b683dc6ab0e0"><font color="#46acc8">MIUI12 MIUI13rnROOT 超简单 小米10 10p 10u 10s 11 11p 11u 安卓10-12均可_哔哩哔哩_bilibili</font></a></p><p style="line-height:1;"><font color="#46acc8"><a href="https://blog.csdn.net/qq_43509120/article/details/121429387"><font color="#46acc8">linux deploy安装指南(升级)_Xin@645的博客-CSDN博客_linux deploy 安装</font><span></span></a></font></p><p style="line-height:1;"><a href="https://github.com/meefik/linuxdeploy/issues/1101"><font color="#46acc8">How to speedrnup linux deploy? · Issue #1101 · meefik/linuxdeploy (github.com)</font></a></p><p style="line-height:1;"><a href="https://mirrors.bfsu.edu.cn/help/debian/"><font color="#46acc8">debian | 镜像站使用帮助 | 北京外国语大学开源软件镜像站 |rnBFSU Open Source Mirror</font></a></p><p style="line-height:1;"><a href="https://blog.csdn.net/m0_59677938/article/details/124986115"><font color="#46acc8">Linux deploy重启的命令_linuxrndeploy 重启_m0_59677938的博客-CSDN博客</font></a></p><p style="line-height:1;"></p><p style="line-height:1;"><a href="https://blog.csdn.net/weixin_73464466/article/details/126654730"><font color="#46acc8">linux deploy安装kali和磁盘扩容_linuxdeploy安装kali_褪色472的博客-CSDN博客</font></a></p>